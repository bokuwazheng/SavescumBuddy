<Window x:Class="SavescumBuddy.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:core="clr-namespace:SavescumBuddy.Core;assembly=SavescumBuddy.Core"
        Title="{Binding Title}" Height="600" Width="1000">
    <Grid>
        <DockPanel>
            <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.Navigation}"/>
            <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.ContentRegion}"/>
        </DockPanel>
        <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.Overlay}"/>
        <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.Autobackuper}" VerticalAlignment="Bottom"/>
    </Grid>
</Window>
